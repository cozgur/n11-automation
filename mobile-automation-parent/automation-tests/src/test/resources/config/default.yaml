# Default test configuration
appium:
  url: "http://127.0.0.1:4723"

timeout:
  implicit: 10
  explicit: 30
  page_load: 60

retry:
  max: 3
  delayMs: 1000

screenshot:
  onFailure: true

reporting:
  log_level: INFO
